# 별빛로그 운영관리(운영자 대시보드) 기획서

> 기존 **권한설정** 탭(관리자 전용)을 **운영관리** 탭으로 확장해, 웹사이트를 운영할 때
> 필요한 기능들을 한곳에 모은 운영자 대시보드로 만드는 계획 문서입니다.

- 문서 버전: v1.0
- 작성일: 2026-07-30
- 대상: 관리자(`teacher_profiles.role = 'admin'`) 계정 전용

---

## 1. 지금 상태

관리자 계정에만 보이는 탭이 두 개 있습니다.

| 탭 | 하는 일 | 데이터원 / API |
|---|---|---|
| **권한설정** | 교사 목록 조회, 등급 변경(일반/유료/관리자), 유료 만료일 설정, 이번 달 AI 사용량 표시 | `teacher_profiles` · `/api/admin/teachers` |
| **알림설정** | 로그인 알림장 등록·수정·삭제·활성 토글, "다시 보지 않기" 교사 수 | `admin_notices` · `/api/admin/notices` |

즉 지금은 "회원 등급 관리 + 공지"만 있습니다. 여기에 **운영에 필요한 지표·관리·점검 기능**을 더해
"운영관리" 탭 하나로 통합하는 것이 목표입니다.

---

## 2. 운영자 대시보드에 보통 들어가는 기능

일반적인 웹서비스 운영자 화면은 아래 8개 영역으로 나뉩니다. 각 영역을 별빛로그에 맞게 정리했습니다.
(✅ 지금 데이터로 바로 가능 / 🟡 일부 가능·보강 필요 / 🔴 신규 테이블·수집 필요)

### 2.1 한눈에 보기 (운영 개요 · KPI)
대시보드 최상단에 핵심 숫자를 카드로 보여줍니다.

| 지표 | 데이터원 | 상태 |
|---|---|---|
| 총 교사 수 / 유료 교사 수 / 관리자 수 | `teacher_profiles` | ✅ |
| 총 학급 수 · 총 학생 수 | `classes` · `students` | ✅ |
| 이번 달 AI 분석 사용량(합계) · 추정 비용 | `ai_usage_logs` | ✅ |
| 오늘/이번 주 활동량(감정·계획·편지·평가·성찰) | `emotion_feeds` · `plan_checks` · `letters` · `eval_reports` · `eval_reflections`의 `created_at` | ✅ |
| 활성 교사(최근 N일 내 활동) · 활성 학생 | `student_sessions` · 활동 테이블 | 🟡 |
| 신규 가입 추세(주/월별) | `teacher_profiles.created_at` | ✅ |

### 2.2 회원 관리 (교사)
지금 권한설정이 하던 일 + 확장.

| 기능 | 상태 |
|---|---|
| 교사 목록·검색·정렬(이름/이메일/등급/가입일) | 🟡 (목록은 있음, 검색·정렬 추가) |
| 등급 변경(일반/유료/관리자), 유료 만료일 설정 | ✅ (기존 권한설정) |
| **유료 만료 임박 교사 목록**(예: 7일 내 만료) | ✅ (`paid_until` 계산) |
| 교사 상세: 가입일·마지막 활동·보유 학급 수·학생 수·이번 달 AI 사용량 | 🟡 |
| 계정 정지/해제, 강제 탈퇴(주의: 학급 데이터 cascade 삭제) | 🔴 |

### 2.3 사용량 · 비용 분석
| 기능 | 상태 |
|---|---|
| AI 사용량 추이(일/주/월), 기능별(성장·홀란드·종합평가) 분해 | ✅ (`ai_usage_logs.feature`) |
| **OpenAI 추정 비용** 표시(사용량 × 단가) | ✅ |
| 교사별 AI 사용 순위(과다 사용 감지) | ✅ |
| 활동 지표 DAU/WAU, 기능별 사용 현황 | 🟡 (활동 테이블 집계) |

### 2.4 공지 · 콘텐츠 관리
| 기능 | 상태 |
|---|---|
| 로그인 알림장 관리 | ✅ (기존 알림설정 흡수) |
| 약관/개인정보처리방침 버전 확인·개정 안내 | 🟡 (`lib/legal.ts` 버전 노출) |
| 서비스 점검(공지 배너) on/off | 🔴 |

### 2.5 데이터 · 라이프사이클 관리
| 기능 | 상태 |
|---|---|
| **학년도 종료 데이터 초기화** 현황·수동 실행(현재 매년 2/말 자동) | 🟡 (크론 존재, 수동 트리거·로그 추가) |
| 특정 학급/교사 데이터 조회·점검 | 🟡 |
| 데이터 백업·내보내기(CSV/JSON) | 🔴 |

### 2.6 운영 건전성 · 모니터링
| 기능 | 상태 |
|---|---|
| 실패 로그 모아보기(예: AI 저장 실패, 연료 지급 실패는 현재 `console.error`) | 🔴 (에러 로그 테이블 필요) |
| 크론 실행 이력(학년말 초기화 등) | 🔴 |
| 최근 회원가입/로그인 이상 징후 | 🟡 |

### 2.7 보안 · 감사(Audit)
| 기능 | 상태 |
|---|---|
| **관리자 행위 감사 로그**(등급 변경·삭제·연료 수동 지급 등 누가/언제/무엇을) | 🔴 (감사 로그 테이블 필요 — 운영자 화면의 핵심) |
| 학생 로그인 잠금 현황·해제 | 🟡 (`student_sessions`/잠금 로직) |
| 교사 로그인 실패·잠금 현황 | 🟡 |

### 2.8 정책 · 설정
| 기능 | 상태 |
|---|---|
| **무료/유료 한도를 코드가 아니라 화면에서 조정**(AI 월 한도, 학급 수 한도) | 🔴 (지금은 `lib/ai/usage.ts` 상수 하드코딩) |
| 기능 플래그(feature flag)로 신기능 점진 오픈 | 🔴 |

---

## 3. 별빛로그 우선순위 제안

지금 데이터로 **바로 만들 수 있는 것부터** 3단계로 나눴습니다.

### 1단계 — MVP (기존 데이터만으로 즉시 구현 가능)
운영자가 "지금 서비스가 어떻게 돌아가는지"를 한 화면에서 보게 하는 것이 목표.
- [ ] 운영 개요 KPI 카드(교사/유료/학급/학생 수, 이번 달 AI 사용량·추정 비용)
- [ ] 회원 관리(기존 권한설정 흡수) + **검색/정렬** + **유료 만료 임박 목록**
- [ ] AI 사용량 분석(기능별·교사별·추이·비용)
- [ ] 알림장 관리(기존 알림설정 흡수)
→ 이 단계는 **새 테이블 없이** `teacher_profiles`·`classes`·`students`·`ai_usage_logs`·활동 테이블 집계로 가능. 신규 API `/api/admin/overview` 하나 정도만 추가.

### 2단계 — 운영 신뢰성
- [ ] **관리자 감사 로그**(신규 테이블 `admin_audit_logs`) — 등급 변경·삭제·연료 지급 기록
- [ ] 학년말 데이터 초기화 현황·수동 실행 + 실행 로그
- [ ] 무료/유료 한도 값을 화면에서 조정(설정 테이블로 이관)
- [ ] 활동 지표(DAU/WAU) 추이

### 3단계 — 확장
- [ ] 실패/에러 로그 대시보드, 크론 이력
- [ ] 데이터 백업·내보내기
- [ ] 결제 연동(현재는 관리자 수동 부여)
- [ ] 기능 플래그, 점검 배너, 고객 문의 수집

---

## 4. 화면 구성 제안

운영관리 탭 하나 안에서 상단 개요 + 하위 섹션(또는 하위 탭)으로 나눕니다.

```
[운영관리]  (관리자 전용)
┌──────────────────────────────────────────────────────────┐
│  운영 개요                                                │
│  ┌────────┬────────┬────────┬────────┬────────┐          │
│  │교사 42 │유료 8  │학급 51 │학생 980│AI 1,240│          │
│  │        │        │        │        │≈ $9    │          │
│  └────────┴────────┴────────┴────────┴────────┘          │
├──────────────────────────────────────────────────────────┤
│  [회원 관리] [사용량·비용] [공지] [데이터·운영] [감사로그]│  ← 하위 섹션
│  ────────────────────────────────────────────────────    │
│  (선택된 섹션 내용)                                       │
└──────────────────────────────────────────────────────────┘
```

- 기존 **권한설정 → "회원 관리" 섹션**으로, **알림설정 → "공지" 섹션**으로 흡수.
- 상단 KPI는 어느 섹션에서든 항상 보이게.

---

## 5. 구현 시 고려사항

1. **권한**: 모든 운영 API는 `requireTeacher()` + `role === 'admin'` 이중 확인. 기존 `/api/admin/*` 패턴 그대로.
2. **감사 로그가 운영자 대시보드의 핵심**: 관리자가 하는 모든 되돌리기 어려운 행위(등급 변경, 강제 삭제, 연료 수동 지급/회수)는 "누가·언제·무엇을·왜"를 남겨야 분쟁·오작동 추적이 됩니다. `admin_audit_logs` 테이블을 2단계에서 우선 도입 권장.
3. **개인정보 최소 노출**: 운영자라도 학생의 감정·평가 원문을 무분별하게 열람하지 않도록, 대시보드는 **집계 수치 중심**으로. 개별 학생 콘텐츠 열람은 최소화하고 접근 시 감사 로그를 남깁니다.
4. **한도 값의 설정화**: 지금 `FREE_MONTHLY_AI_LIMIT` 등은 코드 상수라 바꿀 때마다 배포가 필요합니다. 운영 화면에서 조정하려면 설정 테이블(예: `app_settings`)로 옮기고 코드는 그 값을 읽도록 리팩터링해야 합니다(2단계).
5. **성능**: 개요 KPI는 매번 전체 집계하면 무거워질 수 있으니, `count`(head:true) 쿼리 위주로 하고 필요 시 캐시.

---

## 6. 한 줄 요약

**권한설정 → 운영관리**로 확장하되, 1단계는 *새 테이블 없이* "운영 개요 KPI + 회원관리(검색·만료임박) + AI 사용량·비용 + 공지"만으로 즉시 만들 수 있고, 2단계에서 **감사 로그**와 **한도값 설정화**를 넣는 것이 가장 실용적입니다.
